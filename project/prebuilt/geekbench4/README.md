# The geekbench4 pre-built snap

This example shows how to build a snap from a downloaded pre-built software item (in this case, geekbench4).
This snap uses core26 as the base.

## Building the snap
``` bash
cd snap
snapcraft pack
```
